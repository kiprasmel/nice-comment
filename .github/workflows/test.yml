name: "tests"

on: [push, workflow_dispatch]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - run: yarn --frozen-lockfile
      - run: yarn test
      - run: cat test-results.failed.json 2>/dev/null
 
